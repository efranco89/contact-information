require "rails_helper"

RSpec.describe ContactLogFailuresController, type: :routing do
  # describe "routing" do
  #   it "routes to #index" do
  #     expect(get: "/contact_log_failures").to route_to("contact_log_failures#index")
  #   end
  #
  #   it "routes to #new" do
  #     expect(get: "/contact_log_failures/new").to route_to("contact_log_failures#new")
  #   end
  #
  #   it "routes to #show" do
  #     expect(get: "/contact_log_failures/1").to route_to("contact_log_failures#show", id: "1")
  #   end
  #
  #   it "routes to #edit" do
  #     expect(get: "/contact_log_failures/1/edit").to route_to("contact_log_failures#edit", id: "1")
  #   end
  #
  #
  #   it "routes to #create" do
  #     expect(post: "/contact_log_failures").to route_to("contact_log_failures#create")
  #   end
  #
  #   it "routes to #update via PUT" do
  #     expect(put: "/contact_log_failures/1").to route_to("contact_log_failures#update", id: "1")
  #   end
  #
  #   it "routes to #update via PATCH" do
  #     expect(patch: "/contact_log_failures/1").to route_to("contact_log_failures#update", id: "1")
  #   end
  #
  #   it "routes to #destroy" do
  #     expect(delete: "/contact_log_failures/1").to route_to("contact_log_failures#destroy", id: "1")
  #   end
  # end
end
